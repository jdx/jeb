#!/usr/bin/env node

'use strict';

let Promise  = require('bluebird');
let commands = require('../lib/commands');
let error    = require('../lib/error');

let cmd = commands[process.argv[2]];
cmd = cmd || commands.help;
Promise.resolve(cmd.run(process.argv))
.catch(error.handle);
